// Array A
const A = [0, 1, 2, 13, 4, 5];
const maxA = Math.max(...A);

// Array B
const B = [5, 4, 23, 2, 1, 0];
const maxB = Math.max(...B);

console.log("Maximum value in Array A: " + maxA);
console.log("Maximum value in Array B: " + maxB);
